{
  "name": "fly-mario",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fly Mario / 果蝇闯关 — a fruit-fly brain homage agent that clears a Mario-style platformer offline.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "sim": "vitest run tests/agent-clear.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^6.0.7",
    "vitest": "^3.0.4"
  }
}
